#set text(lang: "es", size: 14pt,)
#set par(justify: true)
#set table.vline(stroke: 1pt)

#align(center)[= Solicitud De Prestamo]

#h(1fr) `______`/`___`/`___`\

Señores de el fondo, yo `___________________________` usuari@ № `_______` del fondo San Javier identificado con cedula de ciudadania № `_________________` solicito un prestamo por el valor de `_________________ `, con el interes de `_____ ` %, tengo la intencion de pagar el prestamo en `_______`mes(es), si mi dinero no llegase a ser suficiente solicito como fiador(es) a (...), con sus respectivas deudas especificadas acontinuacion:

/ `Nombre:`:
/ `Puesto:`:
/ `Deuda:` :

/ `Nombre:`:
/ `Puesto:`:
/ `Deuda:` :

/ `Nombre:`:
/ `Puesto:`:
/ `Deuda:` :

\
\
\
\
\

#grid(
  columns: (50%, 50%), align: (center, center)
)[
  `_________________________`\ usurio del fondo
][
  `_________________________`\ tesorero
]
